# ─────────────────────────────────────────────────────────────
#  fraud-processing (Pretrain) — Container Dependencies
#
#  These run INSIDE the SageMaker container on AWS.
#  Covers SplitData (RDS → feather) and TabPreprocessing steps.
# ─────────────────────────────────────────────────────────────

# AWS
boto3>=1.42.0

# Data
numpy>=2.4.2
pandas>=2.3.3
pyarrow>=19.0.0

# ML (preprocessing only — no torch needed)
scikit-learn>=1.8.0

# Database (RDS PostgreSQL)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
